# Python runtime: 3.7.12 (install separately).
# Major library versions reported for the original experiments.
# Install from the repository root in an activated environment:
# python -m pip install -r requirements.txt

tensorflow==2.7.0
torch==1.10.1
scikit-learn==0.19.2
pandas==1.3.5
numpy==1.21.6
keras==2.7.0
